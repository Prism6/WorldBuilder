# WorldBuilder 개선 방향 문서 (v2.0)

> 기획 문서 v1.0에 대한 개정안. 기존 문서를 대체하지 않고, **무엇을 바꾸고 무엇을 버릴지**를 규정한다.

---

## 0. 한 줄 요약

> **WorldBuilder는 "12개 입력 폼"이 아니라 "설정 간 연결을 관리하는 도구"다.**

v1.0의 핵심 문제는 차별화 기능(연결성·일관성)이 Phase 3에 배치되어 있다는 점이다.
입력 폼만으로는 Notion 템플릿과 구별되지 않는다. **차별점을 Phase 1으로 끌어올린다.**

---

## 1. 진단: v1.0의 구조적 문제

| # | 문제 | 근거 | 결과 |
|---|------|------|------|
| P1 | 데이터 단위가 "요소"라서 연결 표현 불가 | 스키마상 `connections: []`를 채울 대상이 없음 | 2.3 연결성 시각화가 구현 불가능 |
| P2 | 차별화 기능이 후순위 | 연결성 Phase 3, AI Phase 4 | 8주 뒤에야 경쟁력 확인 |
| P3 | 빈 화면 문제 | 12개 빈 폼을 사용자에게 제시 | 첫 사용에서 이탈 |
| P4 | 완성률 지표의 품질 왜곡 | `completion_rate` = 채움 비율 | 글자수 채우기 유도 |
| P5 | SQLite에 JSON 통짜 저장 | `data JSON` 단일 컬럼 | 검색·필터·쿼리 불가 |
| P6 | 일정 비현실성 | 2주 × 4단계 = 8주, 1인 개발 | 병행 업무 고려 시 미달성 |

---

## 2. 개선 방향 (5대 전환)

### 2.1 데이터 구조: 요소 중심 → **엔티티 그래프**

12요소를 데이터 단위에서 **분류 태그**로 강등하고, 실제 저장 단위를 엔티티(노드)로 바꾼다.

```json
{
  "entities": [
    {
      "id": "e1",
      "name": "할렐루야산",
      "type": "location",
      "elements": ["space", "nature"],
      "body": "부유 산맥. [[언옵테늄]]의 반중력 특성으로 부유한다.",
      "links": ["e2"],
      "era": "current"
    }
  ]
}
```

- **엔티티 타입**: `location` / `character`(종족·인물) / `event` / `concept`(규칙·신화·철학) / `item`
- 한 엔티티는 **복수 요소에 동시 소속** 가능 (실제 세계관 설정은 요소 경계를 넘나든다)
- 12요소 뷰는 `elements` 태그로 필터링한 **화면일 뿐**, 저장 구조가 아니다

**효과**: 관계 그래프·검색·상호참조·일관성 체크가 모두 이 하나의 구조에서 파생된다.

### 2.2 `[[위키링크]]` 문법 도입 — 최우선 구현

본문에 `[[언옵테늄]]`을 입력하면:

1. 해당 이름의 엔티티가 없으면 **빈 엔티티 자동 생성**
2. 두 엔티티 간 링크 자동 등록
3. 대상 문서에 **백링크 자동 표시**

구현 난이도는 정규식 파싱 수준이나, 여기서 연결성 그래프·고아 노드 탐지·검색이 전부 무료로 나온다.
**투입 대비 산출이 가장 높은 단일 기능이므로 Phase 1에 배치한다.**

### 2.3 AI: Phase 4 → Phase 1 (역할 재정의)

AI를 **생성기가 아니라 검수자·질문자**로 배치한다.

| 기능 | 역할 | 배치 |
|------|------|------|
| 로그라인 → 12요소 초안 생성 | 빈 화면 문제(P3) 해소. 사용자 행위를 "채우기"에서 "고치기"로 전환 | Phase 1 |
| 모순 탐지 | "기술수준=중세인데 에너지원=핵융합입니다. 의도한 것입니까?" | Phase 2 |
| 동적 후속 질문 | 정적 `questions.json`은 재사용 시 지루해짐. 입력 내용 기반으로 다음 질문 생성 | Phase 2 |

**모순 탐지는 이 앱에서 유료화 가능성이 있는 유일한 기능이다.** 경쟁 서비스가 약한 지점.

### 2.4 완성률 지표 폐기 → 연결 기반 지표

`completion_rate` 제거. 대체 지표:

- **고아 노드 수** — 어디에도 연결되지 않은 설정
- **연결 밀도** — 엔티티당 평균 링크 수
- **미회수 떡밥** — 예언·복선 엔티티 중 결말 엔티티와 미연결인 것

세계관의 품질은 분량이 아니라 **결합도**로 측정한다.

### 2.5 내보내기: 형식 → **용도**

Markdown/PDF는 최소 요건이며 차별화가 아니다. 타겟별 산출물로 확장한다.

| 출력 | 대상 | 우선순위 |
|------|------|----------|
| **LLM 컨텍스트 팩** (세계관을 프롬프트로 압축) | 전 사용자 | 높음 |
| 게임 기획서 세계관 챕터 초안 | 게임 기획자 | 중간 |
| TRPG 핸드아웃 / NPC 시트 | GM | 중간 |
| 캐릭터 시트 | 소설 작가 | 낮음 |

---

## 3. 지도 기능 방침

**결론: 지도 "생성" 기능은 개발하지 않는다.**

### 배제 근거

- 절차적 지도 생성(판구조·수계·바이옴)은 그 자체로 독립 프로젝트 규모
- Azgaar's Fantasy Map Generator 등 무료·오픈소스 경쟁자가 영역을 선점
- AI 이미지 생성은 지도에 부적합 — 지명 텍스트가 깨지고 동일 지형의 재현이 불가능. 지도의 본질은 심미성이 아니라 **정보의 정확성**

### 채택안: 지도 = 엔티티 그래프의 공간 뷰

- 사용자가 지도 이미지를 **업로드**하고, 그 위에 **핀**을 찍는다
- 핀 = `type: location` 엔티티. 클릭 시 해당 문서로 이동, 백링크 표시
- 즉 **신규 데이터 모델이 필요 없다.** 기존 구조의 다른 표현일 뿐
- 외부 생성기(Azgaar 등) 결과물 **임포트**로 생성 수요를 흡수

**구현 주의점**

- 좌표는 픽셀이 아닌 **0~1 정규화 값**으로 저장 → 이미지 교체·줌 배율 변경에도 핀 유지
- Streamlit은 클릭 좌표 취득에 서드파티 컴포넌트(`streamlit-image-coordinates` 등) 필요.
  매 클릭마다 rerun이 발생하므로 **핀 편집 모드를 분리**하여 재렌더 범위를 격리

### 확장안: 시대 레이어

동일 지도에 **시대별 레이어**를 중첩한다 (예: 제국 붕괴 전/후 국경).

- 12요소의 **공간 × 시간 교차**이므로 프레임워크 정합성이 높음
- 구현은 핀·폴리곤에 `era` 태그를 부여하고 필터링하는 수준 → 비용 대비 효과 우수
- 경쟁 서비스에서도 상위 기능에 해당하므로 차별화 여지 있음

---

## 4. 기술 스택 보정

기존 스택(Streamlit + SQLite + JSON) 자체는 MVP 목적에 부합하므로 유지. 단 다음을 보정한다.

### 4.1 DB 정규화 (P5 대응)

```sql
CREATE TABLE entities (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT,              -- location / character / event / concept / item
    elements TEXT,          -- 태그 목록
    body TEXT,
    era TEXT,
    map_x REAL, map_y REAL, -- 정규화 좌표 (0~1)
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE TABLE links (
    src_id TEXT NOT NULL,
    dst_id TEXT NOT NULL,
    relation TEXT,
    PRIMARY KEY (src_id, dst_id)
);
```

프로젝트 단위 JSON은 백업·내보내기 용도로만 유지한다.

### 4.2 Streamlit 상태 관리

- 매 입력마다 전체 rerun이 발생하는 구조 → 폼이 많은 앱에서 `session_state` 관리가 급격히 복잡해진다
- **자동 저장을 debounce 없이 붙이면 DB write가 폭주**한다
- 채택: **명시적 저장 버튼 + 세션 단위 dirty flag**. 자동 저장은 일정 간격 또는 화면 전환 시점에만 수행

---

## 5. 로드맵 재설계

**원칙: 총 기간을 늘리지 말고 MVP 범위를 줄인다.** (P6 대응)

### Phase 1 — 검증 가능한 최소 단위 (2주)

- [ ] 엔티티 데이터 구조 구현 (2.1)
- [ ] `[[위키링크]]` 파싱 + 자동 엔티티 생성 + 백링크 (2.2)
- [ ] 요소 **3~4개만** 활성화 (컨셉 / 공간 / 크리쳐)
- [ ] 로그라인 → 초안 생성 AI 연동 (2.3)
- [ ] JSON 저장·불러오기, Markdown 내보내기

> **12요소 전체 폼은 Phase 1에서 만들지 않는다.** 3개로 구조를 검증한 뒤 복제하는 편이 총 비용이 낮다.

### Phase 2 — 차별화 (2주)

- [ ] 나머지 요소 확장 (구조 검증 후 복제 작업)
- [ ] SQLite 정규화 연동 (4.1)
- [ ] AI 모순 탐지 / 동적 후속 질문
- [ ] 연결 기반 지표 (2.4)
- [ ] 지도 업로드 + 핀 ↔ 엔티티 연결 (3장)

### Phase 3 — 심화 (2주)

- [ ] 연결성 그래프 시각화 (networkx)
- [ ] 시대 레이어 / 영역 폴리곤
- [ ] 용도별 내보내기 — LLM 컨텍스트 팩 우선 (2.5)
- [ ] 장르별 템플릿, 검색

### Phase 4 — 확장 (선택)

- [ ] PDF 설정집, 다국어, 클라우드 동기화, 협업
- [ ] (별도 프로젝트 검토) 절차적 지도 생성

---

## 6. 검증 전략

**전제: 강의 교보재 활용 계획은 없음.** 따라서 자체 검증과 외부 커뮤니티에 의존한다.

### 6.1 도그푸딩 (1순위)

Phase 1 완료 시점에 **진행 중인 본인 창작물 하나를 실제로 끝까지 입력**한다.
사용되지 않는 기능과 병목이 즉시 드러나며, 이는 외부 인터뷰 10건보다 정확한 신호다.

### 6.2 외부 채널

| 채널 | 접근 방식 |
|------|-----------|
| TRPG 디스코드 (GM 커뮤니티) | 완성품 홍보가 아니라 "설정 정리를 어떻게 하시는지" 질문으로 선진입 |
| 웹소설 작가 커뮤니티 | 동일 |
| 게임 기획자 오픈채팅 | 동일 |

### 6.3 공개 형태

- Streamlit Cloud 데모 배포 — **설치 요구가 없는 링크 하나**가 초기 피드백 전환율을 좌우한다
- GitHub README에 동작 GIF 1개

---

## 7. 포지셔닝

| 서비스 | 특성 | 비어 있는 자리 |
|--------|------|----------------|
| World Anvil | 방대·복잡, 영어권 중심 | |
| Campfire | 소설 집필 중심 | |
| Notion 템플릿 | 자유도 높으나 구조·검증 없음 | |
| **WorldBuilder** | | **한국어 + 12요소 프레임워크 + 게임/메타버스 기획 산출물** |

---

## 8. 리스크 갱신

| 위험 | 확률 | 영향 | 대응책 |
|------|------|------|--------|
| 지도 생성 등 고비용 기능으로 인한 지연 | 높음 | 높음 | 생성 기능 배제, 임포트로 대체 (3장) |
| Streamlit rerun에 의한 상태·성능 문제 | 중간 | 중간 | 명시적 저장 + dirty flag, 편집 모드 격리 (4.2) |
| AI 응답 비용·지연 | 중간 | 중간 | 초안 생성은 명시적 버튼 실행, 결과 캐싱 |
| 12요소 프레임워크 출처 문제 | 낮음 | 중간 | README에 워크숍 출처 명시. **상용화·유료화 시 별도 사용 허락 확인 필요** |
| 데이터 손실 | 낮음 | 높음 | JSON 백업 유지 (v1.0 대응책 승계) |

---

## 9. 결정 사항 요약

**채택**

1. 엔티티 그래프 구조로 전환, 12요소는 태그화
2. `[[위키링크]]` 문법 — Phase 1 최우선
3. AI를 검수자·질문자로 조기 배치
4. 완성률 → 연결 기반 지표
5. 지도는 업로드 + 핀 방식, 생성 기능 배제
6. MVP 범위를 요소 3~4개로 축소

**폐기**

- `completion_rate` 지표
- 요소별 고정 dict 스키마
- SQLite 단일 JSON 컬럼 저장
- 지도 생성 기능
- 강의 교보재 활용 방안

**보류 (Phase 3 이후 재검토)**

- PDF 설정집, 다국어, 클라우드 동기화, 협업 기능
- 절차적 지도 생성 (별도 프로젝트로 분리 검토)

---

*문서 버전: 2.0 (개선 방향 개정안)*
*기준 문서: WorldBuilder 기획 문서 v1.0*
